using UnityEngine;
using System.Collections;

public enum GameState {
	HOME,
	READY,
	INGAME,
	FINISH,
	OPTION,
	PAUSE,
}

public enum Rainbow {
	RED,
	ORANGE,
	YELLOW,
	GREEN,
	BLUE,
	INDIGO,
	VIOLET,
	NONE,
}